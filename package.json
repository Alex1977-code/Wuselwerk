{
  "name": "wuselwerk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Wuselwerk — Echtzeit-Puzzle / Truppenrettung. MVP-Prototyp (Web).",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "playwright": "^1.49.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
